#include <string>
#include <vector>

using namespace std;

int solution(string message) {
    int answer = 0;
    vector<string>ret ={"message"}  ;
    answer = message.length()*2;   
    return answer;
}